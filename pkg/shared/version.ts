/**
 * Canonical CLI version for @patdx/pkg.
 *
 * Keep in sync with pkg/deno.json `version`. This file is published to JSR
 * (unlike deno.json), so it is the runtime source of truth for the CLI.
 */
export const PKG_CLI_VERSION = '0.8.0'
